<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add a new App</title>
    <link rel="stylesheet" href="/css/style.css" />
    <script defer src="/js/script.js"></script>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="new-body">
      <h1>Add a new App</h1>

      <form action="/users/<%= user._id %>/applications" method="POST">
        <% if(message) { %>
             <strong class="star">**** <%= message %> ****</strong>
        <% }%>

        <label for="company">Company<span class="star">*</span>:</label>
        <input type="text" id="company" name="company" required placeholder="Company Name"/>

        <label for="title">Title<span class="star">*</span>:</label>
        <input type="text" id="title" name="title" required placeholder="Company Title" />

        <label for="note">Notes:</label>
        <textarea id="notes" name="notes" placeholder="Notes"></textarea>

        <label for="postingLink">Posting Link:</label>
        <input type="text" id="postingLink" name="postingLink" placeholder="https://google.com"/>

        <label for="status">Status:</label>
        <select name="status" id="status">
          <option value="interested">Interested</option>
          <option value="applied">Applied</option>
          <option value="interviewing">Interviewing</option>
          <option value="rejected">Rejected</option>
          <option value="accepted">Accepted</option>
        </select>
         <p><span class="star">*</span> items are required</p>
        <button type="submit">Add Application</button>
      </form>
    </div>
  </body>
</html>
